{
	"scripts": {
		"start": "php -S localhost:8080",
		"build": "cp index.html 404.html",
		"update": "rm -rf ./dist/* && rm -rf .update && git clone git@github.com:fwd/status.git .update && mv .update/dist/* ./dist && mv .update/index.html ./index.html && rm -rf ./.update"
	}
}